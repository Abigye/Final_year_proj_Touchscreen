 To run the application using the source code, 

- install the dependencies  by  running "pip install requirements.txt" in a terminal. Make sure you are in the directory of the application.
- From the same directory, run the application using  the command "python3 EventRecorder.py"  from the terminal. 

